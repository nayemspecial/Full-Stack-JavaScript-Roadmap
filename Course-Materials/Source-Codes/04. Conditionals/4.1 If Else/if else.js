let num = "Nayem";
if ( num > 0 ) {
    console.log(`${num} is a positive number.`);
} else if ( num < 0 ) {
    console.log(`${num} is a negative number.`);
} else if ( num == 0 ) {
    console.log(`${num}. It is zero`);
} else {
    console.log(`${num}. Is not a number.`);
}